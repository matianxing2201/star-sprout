# 复制成 .env.local 再填值。.env.local 不会被提交（.gitignore 的 *.local）。
#
# MiMo TTS 密钥 —— 只在本地跑 `pnpm audio:sync` 生成范读音频时用到。
# 构建期离线生成，密钥不进浏览器、不进仓库。
# 申请地址：https://platform.xiaomimimo.com
MIMO_API_KEY=

# 以下都有默认值，通常不用写：
# MIMO_BASE_URL=https://api.xiaomimimo.com/v1
# MIMO_MODEL=mimo-v2.5-tts
# MIMO_VOICE=冰糖
