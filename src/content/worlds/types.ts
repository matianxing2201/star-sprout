/**
 * 跨学科空间与学习地图的类型出口。
 *
 * 定义本体在 domain（产品的领域语言），这里只做转发，
 * 因此内容包可以继续写 `import type { LearningWorld } from './worlds/types'`。
 */
export type { LearningWorld, WorldMapLayout, WorldMapNode, WorldMapPath } from '@/domain/world/types'
