# 星芽 StarSprout

面向 4–12 岁儿童的自主探索学习空间。孩子在探索地图、互动任务与角色陪伴中主动学习；
家长端提供学习数据与能力分析。本文件只记录**这个上下文独有的词汇**，不放实现细节。

## Language

### 产品与身份

**星芽 StarSprout**：
产品名。只出现在品牌位（浏览器标题、`package.json`、README、家长端页眉、页脚签名）。

**学习世界 LearningWorld**：
孩子在地图上逛的一个地方，例如阅读森林、数学城堡、科学实验室。是 app 内部的**地点名词**，
不是产品名。界面里「去过的学习世界」「回到学习世界」指的都是它。
_Avoid_: 把「学习世界」当产品名使用

**星球**：
年级的隐喻（成长阶梯上一级一颗）。**年级是星球，星球上的地方是学习世界** —— 两个概念刻意不共用同一个词。

### 内容层级

**年级 Grade**：
一级分类，固定八个：中班 → 大班 → 一年级 …… 六年级，顺序锁定。

**领域 Category**：
二级分类，即「知识方向」，例如语言表达、数学启蒙、信息科技。它不是教案。

**主题 Topic**：
三级，一个具体的学习主题，例如《春晓》、量词小魔法师。课程地图上的一个节点。

**课程 Lesson**：
一次 5–10 分钟的探索，由若干学习任务组成。

**学习任务 LearningTask**：
课程内的一步，1–3 分钟。只有六种角色，顺序固定：
角色引入 → 知识发现 → 动手探索 → 小试身手 → 挑战任务 → 领取奖励。

**互动 Interaction**：
学习任务里孩子真正动手做的那件事（盖印章、拖拽归类、连线……）。
判定规则是数据的一部分，不是页面逻辑。

**知识点 KnowledgePoint**：
课程考察的最小单位。家长端的「薄弱知识点」由它命名 —— 没有它，错题无法被称呼。

### 成长

**星星 Star**：完成学习任务获得。
**能量 Energy**：由坚持（连续学习天数）获得。
**徽章 Badge**：达成特定条件获得，条件以数据声明，不是函数。
**等级 Level**：成长阶段（小种子 → 小芽芽 → 小树苗 → …… → 学习世界大师）。

**奖励分级 RewardTier**：
一次反馈的强度等级：轻触 / 答对 / 完成任务 / 完成课程 / 完成章节 / 连续学习。
**页面不得自行加码**，强度由等级决定 —— 这是「不过度奖励」的结构性保证。

**掌握度 Mastery**：某领域下基于作答记录推导的 0–1 数值。
**薄弱知识点**：出错权重最高的知识点，用于生成「今日挑战」推荐。

### 课程地图状态

**未解锁 Locked** / **可以开始 Available** / **正在探索 InProgress** / **已经完成 Completed**。

**没有课程的主题不挡路**：
教案未到的主题（`lessonIds` 为空）在界面上显示「内容准备中」，
但**不参与解锁链** —— 否则它后面所有主题都会被永久锁死。

### 内容与界面

**内容包 ContentPack**：全部课程数据的集合。教案落地的地方。

**语义图标 AppIconName**：
图标名属于领域语言（`flask`、`math-operations`），不是某个图标库的组件名。
词汇表在 domain，渲染映射在 ui，靠编译期对齐。

**场景道具**：
互动里代表实体物件的东西（一只鸟、一根胡萝卜），用 emoji 表达；
与**语义图标**相对 —— 后者的规则是「结构用图标，场景用道具」。

### 声音

**范读**：
一首诗、一首儿歌的示范朗读。它是「听得见的示范」，不是背景音乐 ——
音频只为回答「这句话该怎么念」而存在（在哪里停、哪个词念重）。

**音频片段 AudioClip**：
范读的最小单位：一句角色引导语、一个分句、或整篇。
**离线预生成**：片段在构建期用 TTS 生成成静态文件随站点发布，
运行时只播放文件 —— 不联网、不合成。理由见 `docs/adr/0002`。

**播放的规矩**（三个「不」，由 `features/audio` 统一执行）：
不自动播放（声音是孩子自己点出来的，不是页面推的）；
不叠音（同一时刻只响一个片段，连点两张字卡不会变成重唱）；
不重复解码（同一个 clipId 第二次点是立刻响的）。
「点一下」在识字课里首先是「听它念什么」，判定对错是其次。

### 设计分层（用于讨论视觉重构）

**动效工艺 MotionCraft**：
缓动曲线、时长、回弹幅度、按压反馈、入场起点、循环动画的有无。与视觉身份无关。

**细节工艺 DetailCraft**：
零散但复合的正确性：半透明阴影 vs 实心边框、弹层的 origin、`:active` 的物理反馈。

**视觉语言 VisualLanguage**：
颜色、描边、阴影、圆角、密度、插画感。这是**身份**决策，不是质量决策。

> 讨论「设计风格」时必须区分这三层：前两层是质量（可以有客观对错），
> 第三层是身份（只有取舍）。需求文档第 28 节对第三层有明文约束
> （整体风格为「高品质儿童教育产品 + 游戏化 + 轻动画 + 插画世界」，
> 明确排除「成人 SaaS Dashboard」）。
